def blankScreen(isFilled):
    isFilled = ~isFilled

    ScreeAddress = 
    for i in range(0,) 
    pass

isFilled = 0
while(input):
    if(input=='0'):
        if(isFilled==-1):
            blankScreen(isFilled)
        else:
            continue
    else:
        if(isFilled==0):
            blankScreen(isFilled)
        else:
            continue
        